{
  "name": "hitfactor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "20.11.x"
  },
  "scripts": {
    "========================= Development Scripts ====================": "",
    "start": "concurrently -c \"auto\" --names \"API,WEB\" \"npm run start-api\" \"npm run start-web\"",
    "local": "docker-compose up -d && concurrently -c \"auto\" --names \"API,WEB\" \"npm run start-api-local\" \"npm run start-web\"",
    "start-api-local": "cd api && npm run local",
    "start-api": "cd api && npm start",
    "start-web": "cd web && npm start",
    "test": "tsx --test",
    "====================== Production / Build Scripts ================": "",
    "prod": "cd api && npm run prod",
    "postinstall": "(cd api && npm install); (cd web && npm install && npm run build);",
    "========================== Worker Scripts ========================": "",
    "uploadsWorker": "NODE_OPTIONS='--max-old-space-size=512' node scripts/uploadsWorker.js",
    "uploadsWorkerLocal": "MONGO_URL=mongodb://localhost:27017 NODE_OPTIONS='--max-old-space-size=512' node scripts/uploadsWorker.js",
    "========================= USPSA Import Scripts ============================": "",
    "import": "node scripts/import.js",
    "========================= Database Scripts ============================": "",
    "hydrate-local": "LOCAL_DEV=1 node scripts/hydrateMongo.js",
    "hydrate": "node scripts/hydrateMongo.js",
    "dump": "mongodump $MONGO_URL"
  },
  "devDependencies": {
    "@types/node": "^22.1.0",
    "concurrently": "^8.2.2",
    "tsx": "^4.16.5",
    "zenrows": "^1.3.2"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.637.0",
    "lodash.uniqby": "^4.7.0",
    "undici": "^6.18.2",
    "uuid": "^9.0.1"
  }
}
